trigger:
- master

pool: Default

steps:

- task: CmdLine@2
  inputs:
    script: '$(Agent.HomeDirectory)\\_work\\ToscaCI\\Client\\ToscaCIClient.exe   -m local -t junit -r $(Common.TestResultsDirectory)\\Results.xml)'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: 'C:\Users\mitul.yogesh.doshi\Desktop\vsts-agent-win-x64-2.165.2\_work\1\TestResults\\Results.xml'
    failTaskOnFailedTests: true
    testRunTitle: 'Published Test Results'